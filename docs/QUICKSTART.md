# Quickstart (2 minutes)

1) Build
   cargo build

2) Run demo (defaults)
   cargo run -p agent_cli -- run-demo

3) Verify trace (defaults)
   cargo run -p agent_cli -- verify-trace

Expected:
- OK: wrote target/demo_out
- OK: trace verified
- final state_hash_sha256 printed

